// Handmatig samengestelde oefeningen bij afleveringen van "De Kast van
// Mormon" — zelfde opzet en regels als prisma/podcastContent.ts (zie
// "Podcastafleveringen verwerken" in CLAUDE.md). De afleveringen zelf
// (titel, omschrijving, audio) komen uit de feed; een aflevering zonder
// entry hier staat in de cursus met "oefeningen volgen nog".

import type { PodcastEpisodeSeed } from "./podcastContent";

export const kastVanMormonEpisodes: PodcastEpisodeSeed[] = [];
